matrix:
  include:
    - language: node_js
      before_script:
        - cd detector-api
        - npm install
        - npm run build
      script:
        - npm run test
    
    - language: node_js
      before_script:
        - cd detector-app
        - npm install
        - npm run build
      script:
        - npm run test
